#include<bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n;cin >> n;
	cout << n << endl;
	for(int i = 0;i < n;i++)cout << 1 << ' ';
}
