#include<bits/stdc++.h>
using namespace std;
//size_t f = string::npos
//make pair, tuple

int main(){
      int a,b;
      char c;
      cin >> a >> c >> b;
      if(c == '+')cout << a + b;
      else cout << a - b;
}