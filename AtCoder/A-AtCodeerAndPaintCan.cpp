#include<bits/stdc++.h>
using namespace std;
//size_t f = string::npos
//make pair, tuple

int main(){
      set<int> s;
      int a,b,c;cin >> a >> b >> c;
      s.insert({a,b,c});
      cout << s.size();
}